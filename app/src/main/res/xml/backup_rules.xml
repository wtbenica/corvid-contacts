<?xml version="1.0" encoding="utf-8"?><!--
   Sample backup rules file; uncomment and customize as necessary.
   See https://developer.android.com/guide/topics/data/autobackup
   for details.
   Note: This file is ignored for devices older than API 31
   See https://developer.android.com/about/versions/12/backup-restore
-->
<full-backup-content>
    <!-- Excludes app credentials (server URL, username, app password) from automatic backup
         so a restored backup never silently re-authenticates with potentially stale or revoked credentials. -->
    <exclude
        domain="file"
        path="datastore/auth_prefs.preferences_pb" />
</full-backup-content>